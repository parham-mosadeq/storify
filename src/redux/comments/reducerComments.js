const initState = {
  comments: [],
  error: '',
};

const reducerComments = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducerComments;
